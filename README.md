# 终末地基础设施助手

https://endfield-industry-helper.pages.dev/

一个帮助你查看和管理终末地（Endfield）游戏中各种生产配方的响应式 Web 应用。

## 功能特性

- 📋 **配方浏览** - 查看各类物品的生产配方
- 🧮 **生产计算器** - 计算达到目标产量所需的原材料和中间产物
- 🏭 **分类管理** - 按类型分组显示配方（矿石、植物、药品、电池、装备组件、瓶子）
- 🔄 **多配方支持** - 对于同一产出物的多个配方，第一个正常展示，其余配方默认折叠
- 🔍 **搜索功能** - 快速查找物品和配方
- 📱 **响应式设计** - 适配各种屏幕尺寸
- 🎨 **Material Design** - 使用 Material-UI 组件库
- 🤖 **数据自动生成** - 从 CSV 文件自动生成 TypeScript 类型和数据

## 技术栈

- **框架**: React 18.3.1 + TypeScript 5.7.2
- **UI 库**: Material-UI (MUI) v7.3.7
- **构建工具**: Vite 6.0.5
- **SSG/路由**: Vike 0.4.208
- **测试**: Vitest 4.0.18 + React Testing Library
- **组件开发**: Storybook 8.6.15

## 安装

使用 pnpm 安装依赖：

```bash
pnpm install
```

## 开发

### 启动开发服务器

```bash
pnpm page:dev
```

访问 http://localhost:3000 查看应用。

### 启动 Storybook

```bash
pnpm storybook:dev
```

访问 http://localhost:6006 查看组件文档。

## 构建

构建生产版本：

```bash
pnpm page:build
```

## 测试

运行所有测试：

```bash
pnpm test
```

## 代码质量

### 类型检查

```bash
pnpm lint:ts:type
```

### ESLint 检查

```bash
pnpm lint:ts
```

### 代码格式化

```bash
pnpm format
```

## 项目结构

```
src/
├── receipts/              # 配方和物品数据模块
│   ├── generated/         # 自动生成的 TypeScript 文件（请勿手动编辑）
│   │   ├── items.ts       # 物品数据、类型和枚举
│   │   └── receipts.ts    # 配方数据和类型
│   └── index.ts           # 数据导出和兼容性层
├── pages/                 # 页面组件
│   ├── calculator/        # 生产计算器页面
│   ├── receipts/          # 配方浏览页面
│   └── index/             # 首页
└── ui/                    # UI 组件
    ├── layout/            # 布局组件
    ├── recipe/            # 配方相关组件
    └── routes/            # 路由配置

tools/
└── receipt_gen/           # 数据生成工具
    ├── csv/               # 源数据（CSV 格式）
    │   ├── items.csv      # 物品数据
    │   └── receipts.csv   # 配方数据
    ├── generate_items.ts  # 物品数据生成器
    ├── generate_receipts.ts # 配方数据生成器
    └── index.ts           # 生成器入口

tests/                     # 测试文件
├── receipts/              # 数据完整性测试
├── tools/receipt_gen/     # 生成器测试
├── pages/                 # 页面组件测试
└── ui/                    # UI 组件测试

public/
└── images/
    └── items/             # 物品图片（WebP 格式）
```

## 数据管理

本项目使用 CSV 文件作为数据源，通过自动生成工具转换为 TypeScript 代码。

### 快速开始

1. 编辑 CSV 文件：`tools/receipt_gen/csv/items.csv` 和 `receipts.csv`
2. 运行生成器：`pnpm run autogen`
3. 生成的文件会自动更新到 `src/receipts/generated/`

详细的 CSV 格式说明、数据验证规则和开发指南请参考：
📖 [tools/receipt_gen/README.md](tools/receipt_gen/README.md)

## 贡献

欢迎提交 Issue 和 Pull Request！

## 开源协议

ISC

## 链接

- [GitHub 仓库](https://github.com/fa93hws/endfield-infra-helper)
